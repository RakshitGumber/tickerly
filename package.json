{
  "name": "tickerly",
  "private": true,
  "scripts": {
    "dev:web": "bun --cwd apps/web dev",
    "dev:api": "bun --cwd apps/api dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@duckdb/node-api": "^1.5.4-r.1"
  }
}
